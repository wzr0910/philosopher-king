# 《哲人王》部署成「手机能打开的网址」— 傻瓜步骤

游戏本体是 `single.html`（一个文件，94KB，不联网也能玩）。
下面三选一，把"一个文件"变成一个"网址"，手机浏览器输入就能玩。

---

## 方法一：Netlify Drop（最简单，不用注册账号，3 分钟）

适合：只想最快拿到一个能分享的网址。

1. 电脑浏览器打开 👉 **https://app.netlify.com/drop**
2. 把 `single.html` 这个文件**拖进网页里那个虚线框**。（不要拖文件夹，就拖那一个文件）
3. 等十几秒，页面会显示「Site is live」，并给出一个网址，类似：
   `https://lucky-penguin-123456.netlify.app`
4. 复制这个网址，手机浏览器打开 → 能玩。还能「添加到主屏幕」变成 App。

> 想换名字/绑定自己的域名，点页面右上角「Options → Site settings」里改，免费。

---

## 方法二：GitHub Pages（永久、免费、最稳）

适合：你**有 GitHub 账号**，想要一个长期稳定的网址。

1. 登录 **github.com**，点右上角「＋」→「New repository」，仓库名随便（如 `zhenrenwang`）。
2. 建好后，点「Add file」→「Upload files」，把 `single.html` 传上去，点「Commit changes」。
3. 进仓库 「Settings」→ 左侧「Pages」→ Source 选 **main** 分支、文件夹 **/ (root)** → Save。
4. 等 1 分钟，网址就是：
   `https://你的用户名.github.io/zhenrenwang/single.html`
5. 手机打开这个网址即可。

---

## 方法三：Vercel（也是拖一下，适合以后想持续更新）

1. 打开 **https://vercel.com**，用 GitHub 登录。
2. 点「Add New → Project」，Import 你放 `single.html` 的仓库，或选「Deploy」直接上传。
3. 几十秒后给网址 `https://xxx.vercel.app`。

---

## 想自己改美术 / 换"手绘头像"怎么办

- **改文字、事件、哲学家**：编辑 `data.js`（内容和玩法都在这里）。
- **换真·手绘头像贴图**：头像系统是"插拔式"的。打开 `game.js`，找到
  `function portraitEl(philId, expr, size)`，
  把里面 `window.Portraits.draw(cv, philId, expr)` 换成加载你自己的 PNG 图片即可，
  例如：`cv.style.backgroundImage = "url('art/"+philId+"_"+expr+".png')"`。
  建议每个哲学家 6 贴图（平静/高兴/严肃/愤怒/嘲讽/忧虑）。
- **改完重新打包成单文件**：在本项目目录运行 `node build.js`，会更新 `single.html`。

---

## 给没有电脑、只有手机的情况

手机上的「文件管理」App 直接打开 `single.html` 通常不行（手机浏览器不会以"网页"方式打开本地文件）。
最稳的路径：用上面任意方法生成一个网址，手机访问网址即可。或者在手机装一个支持打开本地 HTML 的浏览器 App（如 iOS 的「Document」、安卓的「Acode」），从里面打开文件。

---

> 备注：当前这个开发环境是**离线沙箱**，没有对外上传权限，所以我没法直接替你生成一个公开网址。
> 但游戏文件是完整可用的，你按上面任意方法花几分钟就能拥有一个自己的网址。
